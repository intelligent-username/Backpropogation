import numpy as np

def fit(weights, data, learning_rate = 0.01, epochs = 200, iterations = 10000) -> tuple:
    """
    Makes the optimal model using backpropagation through gradient descent.
    Returns optimal weights.
    """


def compute_gradients():
    pass

def update_weights(weights, gradients, learning_rate):
    return weights - learning_rate * gradients
